<?
if (eregi(basename(__FILE__), $PHP_SELF))  die ("Security Error ...");

if ($func!="detail") {
    $fields[$findex[urlsvisitadas]]->type="string";
    $fields[$findex[urlsvisitadas]]->dtype="stand";
}

return "";
?>
